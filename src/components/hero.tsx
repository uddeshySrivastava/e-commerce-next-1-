export default function Hero({ title }) {
  return (
    <main className=" border my-2 rounded-md w-full flex justify-center items-center">
      <h1 className="text-3xl"> {title}</h1>
    </main>
  );
}
